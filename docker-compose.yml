services:
    domain_manager:
        build: .
        restart: always
        env_file:
            - .env
        volumes:
            - ./src:/usr/src/app/src
            - /var/run/docker.sock:/var/run/docker.sock
